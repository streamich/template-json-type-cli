import {ObjectValue} from '@jsonjoy.com/json-type/lib/value/ObjectValue';
import {default as add} from './routes/add';

// biome-ignore format: keep each route on a separate line
export const router =
  ( add
  ( ObjectValue.create()));
